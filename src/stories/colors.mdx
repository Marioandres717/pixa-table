import { Meta, ColorPalette, ColorItem } from "@storybook/addon-docs/blocks";
import { config } from "../tailwind/config";
import {primitiveColors} from "../styles/themes"

<Meta title="tokens/Colors" />

<ColorPalette>

    {Object.entries(primitiveColors).map(([name, value], i) => (
       i > 3 &&  <ColorItem key={name} title={name} colors={value} />
    ))}

</ColorPalette>
